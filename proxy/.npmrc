# Worker autonome : registre npm public. N'hérite PAS du scope GitHub Packages
# (@mister-guiiug) du dépôt parent, qui exige NODE_AUTH_TOKEN — absent du job de
# déploiement → sinon `npm i wrangler` ici tente de récupérer la config famille
# et échoue en 401.
registry=https://registry.npmjs.org/
