{
  "name": "supaboss-management-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Proxy CORS sans état pour la Supabase Management API (Cloudflare Worker).",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
